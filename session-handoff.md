# Session handoff

**Current phase:** DEPLOYED + PUBLIC. Production: https://champscope.vercel.app (Vercel project `champscope` ← auto-deploys from GitHub `ziangit/Champscope` — now a public repo, MIT-licensed; Supabase project `amyilzbouobunoegefld`). All jobs run daily (unified 2026-07-08): Vercel watch fallback 06:00 UTC, watcher 06:15, team-source ingest (VGCPastes + pokedata.ovh + MetaVGC) 06:45. Schema incl. `has_rated` + `team_chip_counts()` applied to prod. Vercel Web Analytics enabled (dashboard → Analytics tab).
**Next concrete step:** owner is promoting the project (X + Smogon + Reddit; big window = Worlds, Aug 28–30 2026 San Francisco — first official M-B event; sheets auto-ingest via the pokedata M-B window). Code-wise: Limitless ingest is the best next source (Smogon VGC Major Live 2026 + grassroots weeklies; API verified keyless 2026-07-08), then teamsheet.gg, Smogon-chaos priors, Tampermonkey userscript.

## Done so far
- Harness docs (AGENTS.md holds the content; CLAUDE.md imports it per the Next.js scaffold convention).
- Bootstrap: Next.js 16 App Router + TS + Tailwind v4, idempotent `schema.sql` (seeds verified formats + service_role grants), `vercel.json` daily cron fallback, `.env.example`. Dev/start on port **4977**.
- Showdown client (`lib/showdown/`): `toID`, global polite serial queue (≥600 ms, 3-try backoff, honest UA), typed wrappers modeled from live responses. Format ID `gen9championsvgc2026regmb` verified live.
- Parser (`lib/scout/parse.ts`): log → `ParsedReplay`; 5 real Reg M-B fixtures snapshot-tested + hand-checked; synthetic tests for tie/showteam/Trick/Trace/Metronome edge cases.
- Merge layer (`lib/scout/merge.ts`, `formes.ts`): base-forme fingerprints, frequency-counted reveals with provenance, lead pairs / bring-4, replay refs with ratings, set-variation flag, alt correlation.
- Phase 1 UI: `/scout`, `/player/[userId]`, `/teams`; Pokepaste-style team cards (top-4 moves as the set, 5th+ under "also seen", per-replay `Rating: N`), copyable export capped at 4 moves for teambuilder round-trip.
- Phase 2: `lib/watch.ts` chunked worker (cursor in `scout_runs.cursor`, ~7 s budget, **11 h pass cooldown**), bearer-authed `/api/watch/run`, GH Actions scheduler fires **daily 06:15 UTC** and drives a full pass per run (~3.5 min), `/watch` dashboard.
- `npm run reparse`: re-parses cached `raw_json` on `PARSER_VERSION` bump and rebuilds all team profiles from scratch (also migrates profile-shape changes).
- Local test rig: `supabase start` + `.env.local`; `npx tsx scripts/dev-scout.ts <user>` for CLI scouts. Local DB has its own harvest, independent of prod.
- Tests: 18 passing (`npm test`); `next build` clean.
- Deployment: schema applied via Supabase management API (`POST /v1/projects/<ref>/database/query`); env vars in Vercel; `CRON_SECRET` secret + `WATCH_URL`/`INGEST_URL` variables in GitHub; `ladder-watch` + `team-ingest` enabled. See docs/ARCHITECTURE.md "Production ops".
- Team fetching + matching (2026-07-06): set importer (`lib/scout/import.ts`, inverse of export, sheet-grade provenance, never fabricates EVs); `origin` in the team_profiles identity + `sources` jsonb (dedupe per source key, GIN-indexed); per-host polite queue lanes; chunked `/api/ingest/run` + weekly `team-ingest` workflow; VGCPastes (386 pastes, 376 profiles, 0 errors locally) + pokedata.ovh (NAIC 1,095 masters sheets bulk-ingested locally under regma, 0 species-normalization failures); `match_teams` SQL fn + `/match` page + `GET /api/match` (exact fingerprint + ≥4-overlap, GIN-prefiltered); TeamCard origin badges/sources panel; /teams origin filter; reparse rebuilds only `origin='replay'`. Tests 25 passing.
- 2026-07-08 session: MetaVGC featured-event ingest (`lib/sources/metavgc.ts`, config `METAVGC_EVENTS`) — Champions Arena II = first 24 M-B tournament teams; accordion team cards (native details/summary, CSS-animated both ways, icon-sheet preview strips via `iconStyle`, Expand/Collapse all); /teams rebuilt: DB-side pagination (10/page) + whole-format chip counts (`has_rated` stored generated column + `team_chip_counts()` fn), format selector (M-A now reachable), species jsonb-containment fix, always-visible Reset; /match tournament-first ranking + pagination + query-sprite summary strip; scout: single username only, username+replay mismatch ⇒ no result, invalid replay URL ⇒ error (no /teams fallthrough); sprite fallback for Champions-exclusive formes (`sprite-ids.json`); homepage redesigned (live stats via `homeStats()`, sources legend); gen6 Floette favicon; header GitHub link; README = screenshot feature tour (docs/screenshots/); LICENSE (MIT); Vercel `<Analytics />` in layout. **Screenshot CV feature fully REMOVED** (sharp uninstalled; kept icon-indexes.json + sprite-ids.json, regen via `scripts/build-sprite-data.ts`). Owner's real email scrubbed from HEAD (placeholders) — still in git history; owner declined rewrite so far. Tests 26 passing.
- 2026-07-09: homepage "Data updated N h ago · refreshes daily" line (latest non-null `scout_runs.finished_at` — covers watcher + ingest — added to `homeStats()`; exact UTC on hover via `<time title>`). Verified on prod.
- 2026-07-12: **private replay URLs scout correctly** — pasted `-<password>pw` URLs used to ingest fine but error as "bad replay": the replay server strips the suffix and its JSON reports the base id, so rows are keyed by base id while our cache/membership checks used the pasted id. Fix: `baseReplayId()` in `lib/showdown/id.ts` (mirrors the server's split); fetch keeps the full id (password grants access), all DB lookups use the base id. Username search can never find private replays (search.json is public-only) — URL paste is the only path. Note: pasting a private replay publishes both teams to /teams. **Replay-URL scouts now show only the teams from those replays** (redirect carries `&replays=<baseIds>`; results filter `merged_reveals.replays`, banner says "in the given replay(s)"); username scouts still show the full dossier. Tests 28 passing.
- Capacity check (2026-07-08): prod DB 16 MB of 500 MB free tier; teams ~1.4 KB/row are negligible — replay raw logs (~4.5 KB × ~350/day ≈ 0.5 GB/yr) are the eventual driver; when needed, prune old `raw_json` (keep parsed rows) or Supabase Pro.
- Supabase management API gotcha: requests from Python urllib's default User-Agent get 403 (WAF) — use curl.
- **Regulation ground truth:** VGCPastes M-B tab starts 17 Jun 2026 ⇒ all finished official events incl. NAIC (Jun 12–14) are Reg M-A. `POKEDATA_WINDOWS` maps M-B from 2026-06-15 (empty today; Worlds lands automatically). M-A backfill = one config entry.

## Planned next
- **Screenshot matching** — built 2026-07-06, REMOVED 2026-07-08 (owner: not useful enough, server burden). Full implementation + fixtures in git history; findings in docs/TEAM-SOURCES.md. Kept: data/cv/icon-indexes.json (team-preview icon strips) + data/cv/sprite-ids.json (sprite fallback), regenerated via scripts/build-sprite-data.ts.
- teamsheet.gg ingest; Smogon chaos priors (`usage_stats`); Tampermonkey userscript for /match.

## Open questions
- Owner plans: i18n eventually (next-intl + [locale] segment; game-data names are a separate decision) and China reachability (vercel.app is GFW-blocked; a custom domain is the realistic step — do it before i18n).
- All §10 spec questions answered — see docs/ARCHITECTURE.md. Notable corrections: `|showteam|` appears in ~4% of Champions replays (8/207); Champions ability data diverges from the gen-9 dex (never validate reveals against it).
- Undecided: backfill Reg M-A history — now trivial (add a `POKEDATA_WINDOWS` entry + flip the format active for the watcher, or ingest-only).
- Nice-to-haves not built: rating-trajectory chart on /watch (currently a Δ column), Bo3 format variants in `formats`.
